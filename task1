cat 直接加上文件名是一次显示整个文件
可以使用cat>新文件名来创造一个新的文件

|管道 将左边的输出作为右边的输入
awk: 依次对每一行进行处理
‘’引用代码
$用来表示变量的值
awk '{print $1}' 这句话的意思可以理解成为输出其中那个最前面的元素
zzw
rzj
lsh
hzn
zzy
zt
lsh
zzy
xjj

sort 将文件中的每一行作为一个单位，相互比较，从首字符向后，用ascll进行比较，最后进行升序输出
-r可以实现反向的顺序进行输出
-n 依照数值大小进行排序
hzn
lsh
lsh
rzj
xjj
zt
zzw
zzy
zzy

uniq用于忽略掉文件中存在的重复行，-r可以对其中的出现次数进行输出
-c可以做到对其中的出现次数进行一个统计并且输入

head 可以直接显示文件的多少行，如果没有参数的话会显示所有的行数
可以在后面使用-n （想要的行数）进行更改
-c 可以显示当前文件的前多少个字节head -c 3 xxx1.txt 
处理最后三个以外的所有字节 head -c -3 xxx1.txt 
显示文件名 head -v -n 3 xxx1.txt 
显示多个文件的前5行内容  head -n 3 xxx1.txt xxx2.txt 
grep 画面可以后面跟要找到内容从而发现
 








